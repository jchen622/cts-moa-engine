cache/
settings.json
schedule.log
gui.log
output/
input/
__pycache__/
.DS_Store
**/.DS_Store

# Build artefacts. These embed input/contacts.xlsx, which carries real
# names and a work email address — they must never reach a hosted repo.
# Rebuild with:  python3 build_single_file.py
SEND THIS/
